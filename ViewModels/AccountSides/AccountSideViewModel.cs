namespace ViewModels.AccountSides;

public record AccountSideViewModel(int Id,
									 string Name,
									 string? Mobile,
									 string? Type,
									 string? Address,
									 string? AccountNumber,
									 string? Sheba,
									 string? CardNumber);
